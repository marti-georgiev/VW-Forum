@if (Context.Request.Path.ToString() == "/Thread/Create")
{
    <partial name="_ThreadCreatePartial" />
}
else if (Context.Request.Path.ToString() == "/Community/Create")
{
    <partial name="_CommunityCreatePartial" />
}
